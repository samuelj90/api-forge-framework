<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.apiforgeframework</groupId>
        <artifactId>api-forge-parent</artifactId>
        <version>${revision}</version>
    </parent>

    <groupId>com.company.orders</groupId>
    <artifactId>orders-service</artifactId>
    <name>Example :: Orders Service</name>
    <description>
        Demo service. Only OrdersApiServiceImpl.java is hand-written —
        everything else is generated from the OpenAPI specs.
    </description>

    <dependencies>
        <!--
            Single starter dependency — replaces separate annotations + runtime imports.
            Pulls in autoconfiguration, egress factory, exception handling, validation.
        -->
        <dependency>
            <groupId>org.apiforgeframework</groupId>
            <artifactId>api-forge-spring-boot-starter</artifactId>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- OpenAPI / Swagger UI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- Observability -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>

        <!-- Resilience4j -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- MapStruct -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>

        <!-- H2 for dev/test -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.apiforgeframework</groupId>
            <artifactId>api-forge-test-support</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apiforgeframework</groupId>
                <artifactId>api-forge-maven-plugin</artifactId>
                <version>${revision}</version>
                <executions>

                    <!-- INGRESS: orders API -->
                    <execution>
                        <id>generate-ingress-orders</id>
                        <goals><goal>generate-ingress</goal></goals>
                        <configuration>
                            <specFile>openapi/ingress/orders-api.yaml</specFile>
                            <basePackage>com.company.orders</basePackage>
                            <lombok>true</lombok>
                            <openTelemetry>true</openTelemetry>
                            <validation>true</validation>
                            <logging>true</logging>
                            <!-- generateServiceImpl=true writes scaffold only if file absent -->
                            <generateServiceImpl>true</generateServiceImpl>
                            <manualOperations>
                                <operation>streamOrderEvents</operation>
                            </manualOperations>
                        </configuration>
                    </execution>

                    <!-- EGRESS: inventory upstream -->
                    <execution>
                        <id>generate-egress-inventory</id>
                        <goals><goal>generate-egress</goal></goals>
                        <configuration>
                            <specFile>openapi/egress/inventory-api.yaml</specFile>
                            <clientName>inventory</clientName>
                            <basePackage>com.company.orders</basePackage>
                            <authentication>BEARER_TOKEN</authentication>
                            <circuitBreaker>true</circuitBreaker>
                            <retry>true</retry>
                            <openTelemetry>true</openTelemetry>
                        </configuration>
                    </execution>

                    <!-- EGRESS: payment upstream -->
                    <execution>
                        <id>generate-egress-payment</id>
                        <goals>
                            <goal>generate-egress</goal>
                        </goals>
                        <configuration>
                            <specFile>openapi/egress/payment-api.yaml</specFile>
                            <clientName>payment</clientName>
                            <basePackage>com.company.orders</basePackage>
                            <authentication>OAUTH2_CLIENT_CREDENTIALS</authentication>
                            <circuitBreaker>true</circuitBreaker>
                            <retry>true</retry>
                            <openTelemetry>true</openTelemetry>
                        </configuration>
                    </execution>

                </executions>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <executions>
                    <execution><goals><goal>prepare-agent</goal></goals></execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
